# DragonFly 调试问题记录

---

## 1. CubeMX 点击生成代码时缺少Keil工程等文件

**日期：** 2026-08-01

**问题描述：** 用 CubeMX生成代码时告警提示，没有处理告警内容，继续生成代码后，没有Keil等工程文件

![](https://files.deepseeksvc.com/api/file?file_id=b97e5653-03fc-46b0-88ee-cd475e7e3a08&state=mCJT5AYgRaHx0%2FeQsF2oN8zhK0X8A0v7%2FoJJ1oKvCr5ytb0NN72TSbr8sNL%2FlSKTjCp9JrJjTJTKZH2Eqi91WhKmf7%2FRiydB%2BbGbK6f9eT6Dy1TFGhj9rd%2B10zJeaTLyQDJPO83CXNMOqb5BcJDlUfXhXeNA6pMP0MFx2t%2F4XSx%2B1JwxtYvK2H%2F%2Brq%2Bhqbbr9jsnN%2FealWXsk4JHxQS%2BeLi%2B7oa4qKKxxRcuTS8BgHj89sO%2BIfCVImATh4vs8yWDpscnSw9loDPBu8LyYl34Zba7olbiKn9ZQBA1%2B9JlLqY19yRQeMX7AIq4CohVQVdOA1Dz3NPutPzPefbawkBBBmSCcMIbJKGZtM4%2FqQpqvRuIa9KEeWRlp%2F3B&ty=p)

**原因分析：** 你的本地仓库里缺少 STM32CubeF4 的固件包（版本 1.28.3），而 CubeMX 想联网下载，但检测到你当前的网络连接设置未配置（所以无法自动下载）

**解决方法：** 在CubeMX的菜单栏中点击Help，然后找到网络连接配置，在魔法加持下，选择下图配置后给MX连上了网，然后重新生成代码时自动下载了固件包（版本 1.28.3），下载完成后生成的代码就没问题了

![](C:/Users/Administrator/AppData/Roaming/marktext/images/2026-08-02-22-33-36-image.png)

---

## 2. VSCode + clangd 无法跳转 HAL 函数定义

**日期：** 2026-08-01

**问题描述：** 在 VSCode 中使用 clangd 插件，HAL 库函数无法跳转到定义。

**原因分析：** clangd 需要 `compile_commands.json` 来了解项目的 include 路径、宏定义等编译上下文。

**解决方法：**

1. 在项目根目录创建 `compile_commands.json`，包含所有源文件的编译参数（include 路径、宏定义 `USE_HAL_DRIVER`、`STM32F411xE` 等）
2. 创建 `.clangd` 配置文件，抑制 ARM 特定属性导致的误报
3. 重启 clangd：`Ctrl+Shift+P` → `clangd: Restart language server`

---

## 3. VSCode + clangd 变量无法跳转

**日期：** 2026-08-01

**问题描述：** 函数可以跳转了，但很多变量（如 `huart1`、`htim3`、`uwTick`）仍然无法跳转。

**原因分析：** include 路径使用相对路径，clangd 解析不稳定；缺少 ARM 编译器相关宏定义。

**解决方法：**

1. 将 `compile_commands.json` 中的 include 路径改为绝对路径
2. 新增宏定义 `-D__CC_ARM` 和 `-D__GNUC__`
3. `.clangd` 中添加 `--target=arm-none-eabi` 和 `Index: Background: Build`
4. 重启 clangd 并等待索引完成

---

## 4. printf 串口不输出数据

**日期：** 2026-08-01

**问题描述：** 在 `usart.c` 中添加了 `_write()` 实现 printf 重定向，main.c 中调用 `printf()` 但串口无任何输出。

**原因分析：**

- 本项目使用 **Keil MDK (ARMCC 编译器)**，不是 GCC
- GCC 的 printf 重定向用 `_write()`，Keil ARMCC 需要用 `fputc()`
- 同时 Keil 工程未开启 MicroLIB，导致标准 C 库缺少必要的 syscall 实现

**解决方法：**

1. 在 `usart.c` 中将 `_write()` 替换为 `fputc()`：
   
   ```c
   int fputc(int ch, FILE *f)
   {
    HAL_UART_Transmit(&huart1, (uint8_t *)&ch, 1, HAL_MAX_DELAY);
    return ch;
   }
   ```

2. 在 Keil 工程中开启 MicroLIB：
   
   - 修改 `.uvprojx` 中 `<useUlib>0</useUlib>` 为 `<useUlib>1</useUlib>`
   - 或在 Keil 中：Project → Options for Target → Target → 勾选 "Use MicroLIB"

3. 确保调用 printf 的源文件包含了 `#include <stdio.h>`

---

## 5. Keil 调试时需要点多次 Run 才能正常运行

**日期：** 2026-08-01

**问题描述：** 在 Keil 中调试 STM32 时，代码不能自己跑起来，每次都需要点好几次 Run 才能正常运行。

**原因分析：**

根本原因是**半主机模式（Semihosting）卡住了启动流程**。C 库中的标准 `printf` 函数默认启用半主机模式——让芯片借用电脑的屏幕和键盘做输入输出。程序启动时如果跑到 `printf` 这类函数，就会尝试跟调试器通信，如果调试器响应不及时或配置不对，程序就卡住了。每次点 Run 都像是在手动帮程序和调试器完成握手。

其他可能原因：

- PLL 锁定超时（HSI 上电不稳定，PLL 首次偶发锁定失败）
- 启动代码中等待外部晶振（HSE）起振超时
- 编译器优化等级过高，启动代码被意外优化

**解决方法：**

1. **开启 MicroLIB（最根本的修复）**：在 Keil 中 `Options for Target` → `Target` → 勾选 "Use MicroLIB"。MicroLIB 精简了代码，默认禁用了半主机模式，避免 `printf` 在启动时卡住调试器。

2. **添加启动延时（辅助）**：在 `main.c` 的 `HAL_Init()` 之前加一段延时循环，等待 HSI 稳定后再配置 PLL：
   
   ```c
   /* USER CODE BEGIN 1 */
   for (volatile uint32_t i = 0; i < 1000000; i++);
   /* USER CODE END 1 */
   ```

3. **检查 Keil 调试器复位设置**：`Debug` → `Settings` → `Flash Download` → 勾选 "Reset and Run"，确保下载后自动复位运行。`Debug` → `Settings` → `Reset` 建议设为 `HW RESET`。

4. **检查硬件**：用示波器确认 NRST 引脚电平稳定、BOOT0/BOOT1 电平正确（从主 Flash 启动）。

---

## 6. 电机设置 MOTOR 1 500 后不转

**日期：** 2026-08-01

**问题描述：** 通过串口发送 `MOTOR 1 500` 命令设置电机速度为 500，但电机不转。

**原因分析：** TIM3（电机 PWM 定时器）的配置参数不合理：

- `Prescaler = 0`：分频系数为 1，PWM 频率过高
- `Period = 65535`：自动重载值过大，导致 PWM 频率过低、占空比过小

综合导致实际 PWM 占空比对电机来说过小，设置了目标速度但电机无法转动。

**解决方法：** 修改 TIM3 的 PWM 参数，使 PWM 频率为 **400Hz**，占空比范围为 0-999：

- `Prescaler = 249`（分频 250）
- `Period = 999`（自动重载值 999）



**验证结果：** 设置 `MODE` 为 `MANUAL`（手动模式）后，发送 `MOTOR 1 500` 成功驱动电机运转，问题解决。

---